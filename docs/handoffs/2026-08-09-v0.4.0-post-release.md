---
artifact_contract: "ce-handoff/v1"
created_at: "2026-08-09T16:45:00Z"
title: "folio-resolve v0.4.0 — post-release and consumer propagation"
summary: "v0.4.0 is published and hash-verified; all direct consumers are merged onto the exact pin. Gate 1b remains separate human adjudication work."
keywords: ["folio-resolve", "0.4.0", "recall", "pypi", "consumer-pins", "gate-1b"]
cwd: "/home/damienriehl/Coding Projects/folio-resolve"
resume_focus: "Treat the v0.4.0 release and consumer propagation as complete. Resume Gate 1b only for its open human adjudication decisions."
repository: "folio-resolve"
repo_root_sha: "66481431d28b3228956a6a931097a9979299abe0"
branch: "main"
head: "66481431d28b3228956a6a931097a9979299abe0"
worktree_path: "/home/damienriehl/Coding Projects/folio-resolve"
---

# folio-resolve v0.4.0 — post-release handoff

## Outcome

The recall-quality and runtime work is released. Pull request #2 merged the measured recall
implementation at `c5699e5`; pull request #4 released it as v0.4.0 at merge commit `6648143`.
The package was published through the trusted GitHub release workflow and all three direct
consumers now use an exact `folio-resolve==0.4.0` pin from PyPI.

This supersedes handoffs or Cockpit projections that describe PR #2, the v0.4.0 release, or
consumer propagation as pending.

## Measurement decision

The private, leak-checked aggregate evaluation accepted the recall change:

- tune F1: `+0.007716`
- Firm-2 F1: `+0.010142`
- Firm-2 correct-to-incorrect regressions: `0`
- runtime-only attempt 0003: identical quality outcomes, aggregate runtime `1035.758s`
  (approximately 31% faster than its approved baseline)
- public production-provider benchmark: ontology calls `114 -> 83`, approximately 35% faster

No frozen slice was scored. Do not infer private row-level results from these aggregates or
inspect `eval/reports/experiments.jsonl` to reconstruct them.

## Published artifacts

- GitHub release: <https://github.com/damienriehl/folio-resolve/releases/tag/v0.4.0>
- trusted publish run: <https://github.com/damienriehl/folio-resolve/actions/runs/31324024524>
- wheel SHA256: `1617b00dd07dbff5a439e3098e908a3b8f3c12a2bb0f24018503a6df09ca7c09`
- sdist SHA256: `dfe45082d5f0b71b25bfe750bff2161516458a4ef0656a5838448c2f2df34257`

The published hashes exactly match the locally built release candidates. A fresh index install
of `folio-resolve==0.4.0` passed an import/version smoke test. The project-level PyPI JSON endpoint
briefly remained on 0.3.1 after publication; the version-specific endpoint and fresh install were
used as the authoritative checks.

## Release verification

The release candidate was tested on the real host under Python 3.13:

- pytest: `854 passed, 2 skipped` (856 collected)
- Ruff: passed
- mypy `src`: 26 source files passed
- mypy `eval`: 25 source files passed
- `uv lock --check`: passed
- wheel build/import/recall/determinism/cache-clear smoke: passed

The two skips were environment/data preconditions: the real audit packet/gold v2 was absent and
`openpyxl` is not in the dev extra.

## Direct consumers

All pin and lock changes are merged:

| Consumer | Pull request | Merge commit | Verification |
| --- | --- | --- | --- |
| folio-enrich | [#33](https://github.com/alea-institute/folio-enrich/pull/33) | `29987a6` | 19 integration tests; 903 passed, 43 deselected full backend suite |
| folio-insights | [#1](https://github.com/alea-institute/folio-insights/pull/1) | `599f977` | 17 pin tests; 996 passed, 44 skipped, 4 Dagger-gate tests deselected |
| folio-mapper | [#7](https://github.com/alea-institute/folio-mapper/pull/7) | `dce4a90` | 22 pin tests; 472 passed, 10 skipped core backend suite |

Every consumer lock contains the published v0.4.0 wheel and sdist hashes above; none uses an
editable or working-tree source. Recall remains opt-in, so existing consumer defaults are unchanged.

Environmental exceptions were isolated rather than counted as regressions:

- folio-insights' unfiltered run needs its folio-enrich sibling path and a functional Dagger build
  environment. With the sibling configured and the four Dagger-gate tests deselected, it is green.
- folio-mapper's optional FAISS AVX2 wheel bus-errors on this host. Its complete non-embedding core
  suite and folio-resolve-specific pin suite are green.
- folio-insights has 87 repository-wide pre-existing Ruff findings unrelated to the TOML/lock diff.

## Residual work: Gate 1b is not a release blocker

Cockpit ask `folio-resolve-2026-07-28-gold-audit-gate2` still has q1's adjudication-sheet decisions
open. q2 and q3 are already answered; an answered ask records a decision, not proof that work was
executed. Gate 1b is now a separate human data-quality/adjudication task and does not reopen the
completed v0.4.0 engineering release.

`briefs/on-deck.json` was observed to be stale: it still described q2/q3 and PR #2 as pending.
The briefs repository was concurrently auto-syncing and had dirty generated state, so this session
did not overwrite that projection. Reconcile it through the briefs source workflow, not by an
unguarded `tools/gen-board.py` run.

## Local state and boundaries

- The primary folio-resolve checkout remains intentionally untouched on `docs/codex-orientation`
  at `60a89b3`, with untracked `uv.lock`.
- The F1 worktree remains at
  `/home/damienriehl/Coding Projects/.worktrees/folio-resolve-f1`, branch `feat/f1-eval-loop`, with
  untracked `.context/`. Do not delete or assume it is scratch.
- The disposable 5.1 GB folio-insights test virtual environment created under `/tmp` was removed
  to stay within temporary-disk quota; it is reproducible from `uv.lock` and was later recreated
  for the successful configured suite.
- Do not inspect `eval/data/**` directly, expose row-level output, score the frozen slice, or read
  `eval/reports/experiments.jsonl` contents.
- Do not run folio-enrich's `compare.py`; it writes tracked captures.
- Never inspect `*.pem`, `*.key`, `twin-secrets/`, or `~/.secrets`.
- Never enter or touch `fence-litigation/` or `bayless-aerials/`.

## Maintenance note

The successful publish workflow emitted GitHub Actions' Node 20 deprecation warning and was forced
to Node 24. This is future workflow maintenance, not a release failure.
